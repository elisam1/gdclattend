# TODO: Add Flask API to Desktop Attendance System

- [x] Create src/api.py with Flask app, /welcome endpoint, and start_api_server function to run in background thread
- [x] Install dependencies (Flask is already in requirements.txt)
- [x] Test the /welcome endpoint
- [x] Verify desktop app still works
